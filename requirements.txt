# Install from this repo:
#   pip install -e ".[train]"
#
# Inference-only (used by pip install neurinferno):
#   torch, einops, numpy, huggingface_hub
torch>=2.1
einops>=0.7
numpy>=1.24
huggingface_hub>=0.23
pytorch-lightning>=2.1
gdown>=5.0
