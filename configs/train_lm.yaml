# Stage 1 — ByteLM pretraining
# python -m src.training.train_lm (reads defaults from here via Hydra or argparse)

tier1_dir:   data/tier1_grammar
tier2_dir:   data/tier2_scapy
ckpt_dir:    checkpoints

epochs:      10
batch_size:  128
lr:          3.0e-4
max_len:     256
num_workers: 0
fast_dev:    false
